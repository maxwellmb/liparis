from setuptools import setup

setup(
   name='liparis',
   version='0.1',
   description='data reduction pipline or the ORKID instrument at Keck',
   author='Jack Grossman, Max Millar-Blanchaer',
   author_email='maxmb@ucsb.edu',
   packages=['liparis'],  # would be the same as name
)

